# WebSocket server port (optional, defaults to current page port)
NEXT_PUBLIC_WS_PORT=8080

# WebSocket server URL (optional, auto-derived from page host if not set)
# NEXT_PUBLIC_WS_URL=ws://localhost:8080

# WebSocket host for production CSP headers (required in production)
# NEXT_PUBLIC_WS_HOST=ws.example.com
