# WebSocket server URL (required for client)
# Use wss:// in production
NEXT_PUBLIC_WS_URL=ws://localhost:8080

# HTTP server URL (optional, for future REST endpoints)
NEXT_PUBLIC_HTTP_URL=http://localhost:3000
